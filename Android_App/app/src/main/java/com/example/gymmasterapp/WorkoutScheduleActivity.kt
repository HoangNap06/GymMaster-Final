package com.example.gymmasterapp

import android.os.Bundle
import android.widget.TextView
import androidx.appcompat.app.AppCompatActivity

class WorkoutScheduleActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // Đây là màn hình rỗng, chỉ dùng để tránh lỗi biên dịch Manifest
        setContentView(R.layout.activity_schedule_placeholder)

        // Bạn có thể xóa dòng này nếu bạn đã tạo file activity_schedule_placeholder.xml
    }
}